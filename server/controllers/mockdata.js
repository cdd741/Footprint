const Image = require("../model/Image");
const User = require("../model/User");

async function addData(req, res) {
    // 1. create user
    
    // 2. create post
    //    randomize like number

    // 3. generate post by existing user

   res.status(200).send(query);

}

module.exports = {
    addData,
};
